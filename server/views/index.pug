doctype html
html(lang="en")
  head
    title= Title
    // script(src=assetPath('main.css'))
    script.
      var gon = !{JSON.stringify(gon)};
  body
    nav.navbar.navbar-expand-lg.navbar-light.bg-pizza
      a.navbar-brand(href="/") Pizzeria
      button.navbar-toggler(data-toggle="collapse" data-target="#navbarSupportedContent")
        span.navbar-toggler-icon
      .collapse.navbar-collapse
        ul.navbar-nav.mr-auto
          li.nav-item.active
            a.nav-link(href="/") Home
    .container-fluid
      #delivery
      // block content
    .container-fluid.footer © 2020 for Innoscripta test assignment
    script(src=assetPath('main.js'))
